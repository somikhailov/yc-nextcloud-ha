version: '3'

services:
  db:
    image: postgres:14-alpine
    container_name: postgres
    restart: unless-stopped
    ports:
      - 5432:5432
    volumes:
      - db:/var/lib/postgresql/data
    env_file:
      - db.env
  
volumes:
  db:
